package com.example.library.api.dto.request;

/**
 * Marker request object for borrowing a book.
 *
 * <p>Currently empty but intentionally retained for
 * future extensibility (e.g., borrow duration, metadata).</p>
 */
public class BorrowBookRequest {
}
